name: PostgreSQL Workspace Write Runtime

on:
  push:
    branches: [pg-migration-unified]
    paths:
      - "backend/src/routes/workspaces-runtime.ts"
      - "backend/src/lib/workspace-icon.ts"
      - "backend/src/index.postgres-runtime.ts"
      - "backend/tests/workspaces-runtime-write-pg.test.ts"
      - "backend/tests/postgres-startup-surfaces-pg.test.ts"
      - ".github/workflows/pg-workspace-write-runtime.yml"
  pull_request:
    paths:
      - "backend/src/routes/workspaces-runtime.ts"
      - "backend/src/lib/workspace-icon.ts"
      - "backend/src/index.postgres-runtime.ts"
      - "backend/tests/workspaces-runtime-write-pg.test.ts"
      - "backend/tests/postgres-startup-surfaces-pg.test.ts"
      - ".github/workflows/pg-workspace-write-runtime.yml"
  workflow_dispatch:

jobs:
  workspaces:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: nowen
          POSTGRES_PASSWORD: nowen
          POSTGRES_DB: nowen_note_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U nowen -d nowen_note_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
    env:
      TEST_PG_DATABASE_URL: postgresql://nowen:nowen@127.0.0.1:5432/nowen_note_test
      DATABASE_URL: postgresql://nowen:nowen@127.0.0.1:5432/nowen_note_test
      DB_DRIVER: postgres
      JWT_SECRET: pg-workspace-ci-secret-please-change
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: backend/package-lock.json
      - name: Install backend dependencies
        run: npm ci --no-audit --no-fund
      - name: TypeScript check
        run: npm run build:tsc -- --noEmit
      - name: PostgreSQL workspace create/detail/update lifecycle
        run: node --import tsx --test --test-concurrency=1 tests/workspaces-runtime-write-pg.test.ts
      - name: Authenticated startup regression
        run: node --import tsx --test --test-concurrency=1 tests/postgres-startup-surfaces-pg.test.ts
      - name: Build production backend bundle
        run: npm run build
