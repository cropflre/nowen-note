name: Legacy Knowledge Hierarchy CI

on:
  pull_request:
    branches: [main]
    paths:
      - 'backend/src/routes/notes.ts'
      - 'backend/src/routes/notebooks.ts'
      - 'backend/src/services/legacyKnowledgeHierarchy.ts'
      - 'backend/tests/legacy-knowledge-hierarchy.test.ts'
      - 'scripts/patch_issue464_storage_guard.py'
      - '.github/workflows/legacy-knowledge-hierarchy-ci.yml'

permissions:
  contents: write

jobs:
  backend:
    runs-on: ubuntu-latest
    steps:
      - name: Check out pull request branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0

      - name: Apply conditional storage bootstrap patch
        run: |
          if [ -f scripts/patch_issue464_storage_guard.py ]; then
            python3 scripts/patch_issue464_storage_guard.py
            rm scripts/patch_issue464_storage_guard.py
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add backend/src/services/legacyKnowledgeHierarchy.ts scripts/patch_issue464_storage_guard.py
            git commit -m "fix(knowledge-tree): bootstrap legacy hierarchy storage conditionally"
            git push origin HEAD:${{ github.event.pull_request.head.ref }}
          fi

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: backend/package-lock.json

      - name: Install backend dependencies
        working-directory: backend
        run: npm ci --silent

      - name: Legacy hierarchy coordinator tests
        working-directory: backend
        run: node --import tsx --test tests/legacy-knowledge-hierarchy.test.ts

      - name: Knowledge tree regression tests
        working-directory: backend
        run: node --import tsx --test tests/knowledge-tree.test.ts tests/shared-knowledge-tree.test.ts tests/knowledge-tree-postgres-schema.test.ts

      - name: Legacy note content compatibility
        working-directory: backend
        run: node --import tsx --test tests/markdown-user-content.test.ts tests/notes-content-view.test.ts

      - name: Backend typecheck
        working-directory: backend
        run: npx tsc --noEmit
