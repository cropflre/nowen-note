# Nowen Floating Dock

这是 `floating-layer` UI Slot 的官方零代码示例。四个导航动作全部由 Manifest 声明，
Host 负责渲染、拖拽定位、用户布局持久化和白名单导航执行。

插件没有 JavaScript、CSS、DOM、网络或数据权限。运行时组件 ID 使用
`<pluginId>/<componentId>`，例如 `nowenlab.floating-dock/tasks`。
