import fs from "node:fs";
import path from "node:path";

const root = process.cwd();
const read = (file) => fs.readFileSync(path.join(root, file), "utf8");
const write = (file, content) => {
  const target = path.join(root, file);
  fs.mkdirSync(path.dirname(target), { recursive: true });
  fs.writeFileSync(target, content);
};
const replaceOnce = (file, from, to) => {
  const current = read(file);
  if (!current.includes(from)) throw new Error(`Missing patch anchor in ${file}: ${from.slice(0, 100)}`);
  if (current.indexOf(from) !== current.lastIndexOf(from)) throw new Error(`Ambiguous patch anchor in ${file}`);
  write(file, current.replace(from, to));
};

// Backend manifest/types: Note Template + Prompt Pack are static host-owned contributions.
replaceOnce(
  "backend/src/plugins/types.ts",
  `export interface PluginNoteThemeContribution {\n  id: string;\n  name: string;\n  description?: string;\n  base?: \"nowen.default\";\n  modes: { light: PluginNoteThemeTokens; dark?: PluginNoteThemeTokens };\n}\n`,
  `export interface PluginNoteThemeContribution {\n  id: string;\n  name: string;\n  description?: string;\n  base?: \"nowen.default\";\n  modes: { light: PluginNoteThemeTokens; dark?: PluginNoteThemeTokens };\n}\nexport interface PluginStaticInputField {\n  id: string;\n  label?: string;\n  type: \"string\" | \"number\" | \"boolean\";\n  required?: boolean;\n  default?: string | number | boolean;\n}\nexport interface PluginNoteTemplateContribution {\n  id: string;\n  name: string;\n  description?: string;\n  contentFormat: \"markdown\" | \"tiptap-json\";\n  body: string;\n  variables?: PluginStaticInputField[];\n  suggestedTags?: string[];\n  documentTypes?: Array<\"note\" | \"markdown\">;\n}\nexport interface PluginPromptPackContribution {\n  id: string;\n  name: string;\n  description?: string;\n  prompt: string;\n  inputs?: PluginStaticInputField[];\n  context?: Array<\"title\" | \"note\" | \"selection\" | \"tags\">;\n  outputMode?: \"text\" | \"markdown\" | \"replace-selection\" | \"append\";\n  uiPlatform?: Array<\"web\" | \"desktop\" | \"android\" | \"ios\">;\n}\n`,
);
replaceOnce(
  "backend/src/plugins/types.ts",
  `  noteThemes?: PluginNoteThemeContribution[];\n}\n\nexport interface PluginDeclarativeContributionManifest {\n  settings?: PluginSettingContribution[];\n  automationTemplates?: PluginAutomationTemplateContribution[];\n  noteThemes?: PluginNoteThemeContribution[];\n}`,
  `  noteThemes?: PluginNoteThemeContribution[];\n  noteTemplates?: PluginNoteTemplateContribution[];\n  promptPacks?: PluginPromptPackContribution[];\n}\n\nexport interface PluginDeclarativeContributionManifest {\n  settings?: PluginSettingContribution[];\n  automationTemplates?: PluginAutomationTemplateContribution[];\n  noteThemes?: PluginNoteThemeContribution[];\n  noteTemplates?: PluginNoteTemplateContribution[];\n  promptPacks?: PluginPromptPackContribution[];\n}`,
);

replaceOnce(
  "backend/src/plugins/manifest.ts",
  `const v2BaseShape = {`,
  `const staticInputFieldSchema = z.object({\n  id: z.string().regex(/^[a-z][a-z0-9_-]{0,63}$/),\n  label: z.string().min(1).max(100).optional(),\n  type: z.enum([\"string\", \"number\", \"boolean\"]),\n  required: z.boolean().optional(),\n  default: z.union([z.string(), z.number(), z.boolean()]).optional(),\n}).strict().superRefine((field, ctx) => {\n  if (field.default !== undefined && typeof field.default !== field.type) {\n    ctx.addIssue({ code: z.ZodIssueCode.custom, path: [\"default\"], message: \"变量默认值类型与 type 不一致\" });\n  }\n});\nconst unsafeStaticContent = /(?:<\\s*(?:script|iframe|object)\\b|javascript\\s*:|data\\s*:\\s*text\\/html)/i;\nconst noteTemplateSchema = z.object({\n  id: z.string().regex(/^[a-z][a-z0-9-]{0,63}$/),\n  name: z.string().min(1).max(100),\n  description: z.string().max(500).optional(),\n  contentFormat: z.enum([\"markdown\", \"tiptap-json\"]),\n  body: z.string().min(1),\n  variables: z.array(staticInputFieldSchema).max(50).optional(),\n  suggestedTags: z.array(z.string().min(1).max(64)).max(20).optional(),\n  documentTypes: z.array(z.enum([\"note\", \"markdown\"])).min(1).max(2).optional(),\n}).strict().superRefine((template, ctx) => {\n  if (Buffer.byteLength(template.body, \"utf8\") > 262144) ctx.addIssue({ code: z.ZodIssueCode.custom, path: [\"body\"], message: \"Note Template body 不能超过 256KiB\" });\n  if (unsafeStaticContent.test(template.body)) ctx.addIssue({ code: z.ZodIssueCode.custom, path: [\"body\"], message: \"Note Template 包含不安全内容\" });\n  if (template.contentFormat === \"tiptap-json\") {\n    try { JSON.parse(template.body); } catch { ctx.addIssue({ code: z.ZodIssueCode.custom, path: [\"body\"], message: \"tiptap-json 模板必须是合法 JSON\" }); }\n  }\n  const ids = (template.variables || []).map((item) => item.id);\n  if (new Set(ids).size !== ids.length) ctx.addIssue({ code: z.ZodIssueCode.custom, path: [\"variables\"], message: \"模板变量 id 不能重复\" });\n});\nconst promptPackSchema = z.object({\n  id: z.string().regex(/^[a-z][a-z0-9-]{0,63}$/),\n  name: z.string().min(1).max(100),\n  description: z.string().max(500).optional(),\n  prompt: z.string().min(1),\n  inputs: z.array(staticInputFieldSchema).max(50).optional(),\n  context: z.array(z.enum([\"title\", \"note\", \"selection\", \"tags\"])).max(4).optional(),\n  outputMode: z.enum([\"text\", \"markdown\", \"replace-selection\", \"append\"]).default(\"markdown\"),\n  uiPlatform: z.array(z.enum([\"web\", \"desktop\", \"android\", \"ios\"])).max(4).optional(),\n}).strict().superRefine((prompt, ctx) => {\n  if (Buffer.byteLength(prompt.prompt, \"utf8\") > 65536) ctx.addIssue({ code: z.ZodIssueCode.custom, path: [\"prompt\"], message: \"Prompt 不能超过 64KiB\" });\n  if (unsafeStaticContent.test(prompt.prompt)) ctx.addIssue({ code: z.ZodIssueCode.custom, path: [\"prompt\"], message: \"Prompt 包含不安全内容\" });\n  const ids = (prompt.inputs || []).map((item) => item.id);\n  if (new Set(ids).size !== ids.length) ctx.addIssue({ code: z.ZodIssueCode.custom, path: [\"inputs\"], message: \"Prompt input id 不能重复\" });\n});\n\nconst v2BaseShape = {`,
);
replaceOnce(
  "backend/src/plugins/manifest.ts",
  `  noteThemes: z.array(noteThemeSchema).max(20).optional(),\n}).strict().optional();`,
  `  noteThemes: z.array(noteThemeSchema).max(20).optional(),\n  noteTemplates: z.array(noteTemplateSchema).max(100).optional(),\n  promptPacks: z.array(promptPackSchema).max(100).optional(),\n}).strict().optional();`,
);
replaceOnce(
  "backend/src/plugins/manifest.ts",
  `  noteThemes: z.array(noteThemeSchema).max(20).optional(),\n}).strict().superRefine((contributes, ctx) => {\n  if (!(contributes.settings?.length || contributes.automationTemplates?.length || contributes.noteThemes?.length)) {`,
  `  noteThemes: z.array(noteThemeSchema).max(20).optional(),\n  noteTemplates: z.array(noteTemplateSchema).max(100).optional(),\n  promptPacks: z.array(promptPackSchema).max(100).optional(),\n}).strict().superRefine((contributes, ctx) => {\n  if (!(contributes.settings?.length || contributes.automationTemplates?.length || contributes.noteThemes?.length || contributes.noteTemplates?.length || contributes.promptPacks?.length)) {`,
);
replaceOnce(
  "backend/src/plugins/manifest.ts",
  `  if (new Set(noteThemeIds).size !== noteThemeIds.length) ctx.addIssue({ code: z.ZodIssueCode.custom, path: [\"contributes\", \"noteThemes\"], message: \"Note Theme id 不能重复\" });`,
  `  if (new Set(noteThemeIds).size !== noteThemeIds.length) ctx.addIssue({ code: z.ZodIssueCode.custom, path: [\"contributes\", \"noteThemes\"], message: \"Note Theme id 不能重复\" });\n  const noteTemplateIds = (manifest.contributes?.noteTemplates || []).map((item) => item.id);\n  if (new Set(noteTemplateIds).size !== noteTemplateIds.length) ctx.addIssue({ code: z.ZodIssueCode.custom, path: [\"contributes\", \"noteTemplates\"], message: \"Note Template id 不能重复\" });\n  const promptPackIds = (manifest.contributes?.promptPacks || []).map((item) => item.id);\n  if (new Set(promptPackIds).size !== promptPackIds.length) ctx.addIssue({ code: z.ZodIssueCode.custom, path: [\"contributes\", \"promptPacks\"], message: \"Prompt Pack id 不能重复\" });`,
);

// Frontend API contract mirrors the host-owned static contributions.
replaceOnce(
  "frontend/src/lib/pluginApi.ts",
  `export interface PluginNoteThemeContribution {\n  id: string; name: string; description?: string; base?: \"nowen.default\";\n  modes: { light: PluginNoteThemeTokens; dark?: PluginNoteThemeTokens };\n}\n`,
  `export interface PluginNoteThemeContribution {\n  id: string; name: string; description?: string; base?: \"nowen.default\";\n  modes: { light: PluginNoteThemeTokens; dark?: PluginNoteThemeTokens };\n}\nexport interface PluginStaticInputField { id: string; label?: string; type: \"string\" | \"number\" | \"boolean\"; required?: boolean; default?: string | number | boolean }\nexport interface PluginNoteTemplateContribution {\n  id: string; name: string; description?: string; contentFormat: \"markdown\" | \"tiptap-json\"; body: string;\n  variables?: PluginStaticInputField[]; suggestedTags?: string[]; documentTypes?: Array<\"note\" | \"markdown\">;\n}\nexport interface PluginPromptPackContribution {\n  id: string; name: string; description?: string; prompt: string; inputs?: PluginStaticInputField[];\n  context?: Array<\"title\" | \"note\" | \"selection\" | \"tags\">;\n  outputMode?: \"text\" | \"markdown\" | \"replace-selection\" | \"append\";\n  uiPlatform?: Array<\"web\" | \"desktop\" | \"android\" | \"ios\">;\n}\n`,
);
replaceOnce(
  "frontend/src/lib/pluginApi.ts",
  `  noteThemes?: PluginNoteThemeContribution[];\n  [key: string]: unknown;`,
  `  noteThemes?: PluginNoteThemeContribution[];\n  noteTemplates?: PluginNoteTemplateContribution[];\n  promptPacks?: PluginPromptPackContribution[];\n  [key: string]: unknown;`,
);
replaceOnce(
  "frontend/src/lib/pluginApi.ts",
  `    noteThemes?: PluginNoteThemeContribution[];\n  };`,
  `    noteThemes?: PluginNoteThemeContribution[];\n    noteTemplates?: PluginNoteTemplateContribution[];\n    promptPacks?: PluginPromptPackContribution[];\n  };`,
);

write("backend/src/plugins/contributions/noteTemplateContribution.ts", `import { getDb } from \"../../db/schema.js\";\nimport { syncReferences } from \"../../lib/attachmentRefs.js\";\nimport { rebuildBlockAuthorityStore } from \"../../lib/blockAuthorityStore.js\";\nimport { syncNoteBlocks } from \"../../lib/noteBlocks.js\";\nimport { syncNoteLinks } from \"../../lib/noteLinks.js\";\nimport { getUserWorkspaceRole, isSystemAdmin } from \"../../middleware/acl.js\";\nimport { createKnowledgeChild, type KnowledgeTreeNode } from \"../../services/knowledgeTree.js\";\nimport { rebuildYjsSubdocumentsIfEnabled } from \"../../services/yjs-subdocuments.js\";\nimport type { PluginNoteTemplateContribution, PluginStaticInputField } from \"../types.js\";\n\nexport class PluginNoteTemplateError extends Error {\n  constructor(public readonly code: string, message: string) { super(message); this.name = \"PluginNoteTemplateError\"; }\n}\n\nfunction workspaceId(value: unknown): string | null {\n  const normalized = String(value || \"\").trim();\n  return !normalized || normalized === \"personal\" || normalized === \"null\" ? null : normalized;\n}\nfunction requireWorkspace(userId: string, id: string | null) {\n  if (!id || isSystemAdmin(userId) || getUserWorkspaceRole(id, userId)) return;\n  throw new PluginNoteTemplateError(\"PLUGIN_TEMPLATE_WORKSPACE_FORBIDDEN\", \"无权在该工作区使用插件模板\");\n}\nfunction valuesFor(fields: PluginStaticInputField[] | undefined, input: Record<string, unknown>) {\n  const values: Record<string, string | number | boolean> = {};\n  for (const field of fields || []) {\n    const value = input[field.id] ?? field.default;\n    if (value === undefined || value === null || value === \"\") {\n      if (field.required) throw new PluginNoteTemplateError(\"PLUGIN_TEMPLATE_VARIABLE_REQUIRED\", \`缺少模板变量: \${field.id}\`);\n      values[field.id] = \"\";\n      continue;\n    }\n    if (typeof value !== field.type) throw new PluginNoteTemplateError(\"PLUGIN_TEMPLATE_VARIABLE_INVALID\", \`模板变量类型错误: \${field.id}\`);\n    values[field.id] = value as string | number | boolean;\n  }\n  return values;\n}\nfunction renderText(source: string, values: Record<string, string | number | boolean>): string {\n  return source.replace(/\\{\\{\\s*([a-z][a-z0-9_-]{0,63})\\s*\\}\\}/gi, (_all, key: string) => key in values ? String(values[key]) : \`{{\${key}}}\`);\n}\nfunction renderBody(template: PluginNoteTemplateContribution, input: Record<string, unknown>): { content: string; contentText: string } {\n  const values = valuesFor(template.variables, input);\n  if (template.contentFormat === \"markdown\") {\n    const content = renderText(template.body, values);\n    return { content, contentText: content };\n  }\n  let document: unknown;\n  try { document = JSON.parse(template.body); } catch { throw new PluginNoteTemplateError(\"PLUGIN_TEMPLATE_BODY_INVALID\", \"富文本模板 JSON 无效\"); }\n  const textParts: string[] = [];\n  const visit = (node: unknown): unknown => {\n    if (Array.isArray(node)) return node.map(visit);\n    if (!node || typeof node !== \"object\") return node;\n    const output: Record<string, unknown> = {};\n    for (const [key, value] of Object.entries(node as Record<string, unknown>)) {\n      if (key === \"text\" && typeof value === \"string\") { const text = renderText(value, values); output[key] = text; textParts.push(text); }\n      else output[key] = visit(value);\n    }\n    return output;\n  };\n  const rendered = visit(document);\n  return { content: JSON.stringify(rendered), contentText: textParts.join(\" \") };\n}\n\nexport function createNoteFromPluginTemplate(input: {\n  userId: string; workspaceId?: string | null; parentId: string | null; template: PluginNoteTemplateContribution; values?: Record<string, unknown>;\n}): { noteId: string; node: KnowledgeTreeNode } {\n  const db = getDb();\n  const scope = workspaceId(input.workspaceId);\n  requireWorkspace(input.userId, scope);\n  if (input.parentId) {\n    const parent = db.prepare(\"SELECT workspaceId FROM knowledge_tree_nodes WHERE id=? AND isDeleted=0\").get(input.parentId) as { workspaceId: string | null } | undefined;\n    if (!parent) throw new PluginNoteTemplateError(\"PLUGIN_TEMPLATE_PARENT_NOT_FOUND\", \"目标目录不存在\");\n    if (workspaceId(parent.workspaceId) !== scope) throw new PluginNoteTemplateError(\"PLUGIN_TEMPLATE_SCOPE_MISMATCH\", \"模板与目标目录不在同一空间\");\n  }\n  const rendered = renderBody(input.template, input.values || {});\n  const node = db.transaction(() => {\n    const created = createKnowledgeChild({ actorUserId: input.userId, workspaceId: scope, parentId: input.parentId, nodeType: input.template.contentFormat === \"markdown\" ? \"markdown\" : \"note\", title: input.template.name, db });\n    const note = db.prepare(\"SELECT version FROM notes WHERE id=?\").get(created.resourceId) as { version: number } | undefined;\n    if (!note) throw new PluginNoteTemplateError(\"PLUGIN_TEMPLATE_NOTE_CREATE_FAILED\", \"模板笔记创建失败\");\n    const synced = syncNoteBlocks(db, created.resourceId, rendered.content, input.template.contentFormat);\n    db.prepare(\"UPDATE notes SET content=?,contentText=?,contentFormat=?,updatedAt=datetime('now') WHERE id=?\").run(synced.content, rendered.contentText, input.template.contentFormat, created.resourceId);\n    syncReferences(db, created.resourceId, synced.content);\n    syncNoteLinks(db, input.userId, created.resourceId, synced.content);\n    rebuildBlockAuthorityStore(db, created.resourceId, synced.content, input.template.contentFormat, { noteVersion: note.version, operationType: \"create\" });\n    rebuildYjsSubdocumentsIfEnabled(db, created.resourceId, synced.content, input.template.contentFormat);\n    return created;\n  })();\n  return { noteId: node.resourceId, node };\n}\n`);

write("frontend/src/lib/pluginTemplateRegistry.ts", `import type { PluginContributionRecord, PluginNoteTemplateContribution } from \"@/lib/pluginApi\";\n\nconst PLUGIN_ID_RE = /^[a-z0-9]+(?:[.-][a-z0-9]+)+$/;\nconst LOCAL_ID_RE = /^[a-z][a-z0-9-]{0,63}$/;\nconst UNSAFE = /(?:<\\s*(?:script|iframe|object)\\b|javascript\\s*:|data\\s*:\\s*text\\/html)/i;\nexport interface RegisteredPluginNoteTemplate extends PluginNoteTemplateContribution { runtimeId: string; pluginId: string; publisher?: string }\nlet templates: RegisteredPluginNoteTemplate[] = [];\nexport function replacePluginNoteTemplates(records: PluginContributionRecord[]): number {\n  const next: RegisteredPluginNoteTemplate[] = [];\n  for (const record of records) {\n    const pluginId = String(record.pluginId || \"\").toLowerCase();\n    if (!PLUGIN_ID_RE.test(pluginId) || !Array.isArray(record.noteTemplates)) continue;\n    for (const item of record.noteTemplates) {\n      if (!item || !LOCAL_ID_RE.test(item.id) || typeof item.name !== \"string\" || typeof item.body !== \"string\" || UNSAFE.test(item.body)) continue;\n      next.push({ ...item, runtimeId: \`\${pluginId}/\${item.id}\`, pluginId, publisher: record.publisher });\n    }\n  }\n  templates = next;\n  return next.length;\n}\nexport function listPluginNoteTemplates(): RegisteredPluginNoteTemplate[] { return [...templates]; }\n`);

write("frontend/src/lib/pluginPromptRegistry.ts", `import type { PluginContributionRecord, PluginPromptPackContribution } from \"@/lib/pluginApi\";\n\nconst PLUGIN_ID_RE = /^[a-z0-9]+(?:[.-][a-z0-9]+)+$/;\nconst LOCAL_ID_RE = /^[a-z][a-z0-9-]{0,63}$/;\nexport interface RegisteredPluginPrompt extends PluginPromptPackContribution { runtimeId: string; pluginId: string; publisher?: string }\nlet prompts: RegisteredPluginPrompt[] = [];\nexport function replacePluginPrompts(records: PluginContributionRecord[]): number {\n  const next: RegisteredPluginPrompt[] = [];\n  for (const record of records) {\n    const pluginId = String(record.pluginId || \"\").toLowerCase();\n    if (!PLUGIN_ID_RE.test(pluginId) || !Array.isArray(record.promptPacks)) continue;\n    for (const item of record.promptPacks) {\n      if (!item || !LOCAL_ID_RE.test(item.id) || typeof item.name !== \"string\" || typeof item.prompt !== \"string\") continue;\n      next.push({ ...item, runtimeId: \`\${pluginId}/\${item.id}\`, pluginId, publisher: record.publisher });\n    }\n  }\n  prompts = next;\n  return next.length;\n}\nexport function listPluginPrompts(platform?: \"web\" | \"desktop\" | \"android\" | \"ios\"): RegisteredPluginPrompt[] {\n  return prompts.filter((item) => !platform || !item.uiPlatform?.length || item.uiPlatform.includes(platform));\n}\nexport function renderPluginPrompt(item: RegisteredPluginPrompt, values: Record<string, unknown>): string {\n  for (const field of item.inputs || []) {\n    const value = values[field.id] ?? field.default;\n    if (field.required && (value === undefined || value === null || value === \"\")) throw new Error(\`缺少 Prompt 输入: \${field.id}\`);\n    if (value !== undefined && value !== null && typeof value !== field.type) throw new Error(\`Prompt 输入类型错误: \${field.id}\`);\n  }\n  return item.prompt.replace(/\\{\\{\\s*([a-z][a-z0-9_-]{0,63})\\s*\\}\\}/gi, (_all, key: string) => {\n    const field = item.inputs?.find((entry) => entry.id === key);\n    const value = values[key] ?? field?.default;\n    return value === undefined || value === null ? \`{{\${key}}}\` : String(value);\n  });\n}\n`);

write("examples/plugins/productivity-pack/manifest.json", JSON.stringify({
  id: "nowenlab.productivity-pack", publisher: "nowenlab", name: "Nowen Productivity Pack", description: "Official zero-code note templates and prompt pack dogfood", version: "1.0.0", apiVersion: 2, engines: { nowen: ">=1.6.0" }, runtime: "declarative", categories: ["productivity"], repository: "https://github.com/cropflre/nowen-note", license: "MIT", permissions: [], contributes: {
    noteTemplates: [
      { id: "meeting-notes", name: "会议纪要", description: "结构化会议记录", contentFormat: "markdown", body: "# {{title}}\n\n## 目标\n\n{{goal}}\n\n## 讨论要点\n\n- \n\n## 决策\n\n- \n\n## Action Items\n\n- [ ] ", variables: [{ id: "title", label: "会议主题", type: "string", required: true }, { id: "goal", label: "会议目标", type: "string", default: "" }], suggestedTags: ["会议"], documentTypes: ["markdown"] },
      { id: "weekly-review", name: "周复盘", description: "每周复盘与下周计划", contentFormat: "markdown", body: "# {{week}} 周复盘\n\n## 本周完成\n\n- \n\n## 阻塞与风险\n\n- \n\n## 下周优先级\n\n1. ", variables: [{ id: "week", label: "周次", type: "string", required: true }], suggestedTags: ["复盘"], documentTypes: ["markdown"] }
    ],
    promptPacks: [
      { id: "summarize-note", name: "总结当前笔记", description: "将当前笔记压缩成重点、结论和待办", prompt: "请根据当前笔记内容，输出：1. 三条核心要点；2. 明确结论；3. 可执行待办。不要补充原文不存在的事实。", context: ["title", "note", "tags"], outputMode: "markdown", uiPlatform: ["web", "desktop", "android", "ios"] },
      { id: "rewrite-selection", name: "润色选中文本", description: "保持原意并提升清晰度", prompt: "请润色当前选中的文本。保持事实和原意，不新增未经提供的信息。语气要求：{{tone}}。", inputs: [{ id: "tone", label: "语气", type: "string", default: "清晰、专业、自然" }], context: ["selection"], outputMode: "replace-selection", uiPlatform: ["web", "desktop", "android", "ios"] }
    ]
  }
}, null, 2) + "\n");
write("examples/plugins/productivity-pack/README.md", `# Nowen Productivity Pack\n\nV2.1 Note Template + Prompt Pack 的官方零代码 dogfood。包内只有 Manifest，不包含 JavaScript、WASM、CSS、AI Key 或数据权限。\n\n- Note Template 由 Host 在用户明确点击后创建笔记。\n- Prompt Pack 只声明输入、上下文和输出模式；AI Provider 密钥始终由 Host 保管。\n- 禁用或卸载插件只撤销模板/Prompt 入口，不删除已经创建的笔记。\n`);

write("docs/plugin-platform/note-template-contribution.md", `# Note Template Contribution\n\nV2.1 的 \`contributes.noteTemplates\` 是零代码静态资源。Host 校验模板大小、变量 Schema、文档类型和危险内容；插件本身不获得 \`notes:write\`。只有用户明确选择模板时，Host 才创建笔记。\n\n运行时 ID 使用 \`<pluginId>/<templateId>\`。单插件最多 100 个模板，body 最大 256KiB。插件禁用/卸载后入口消失，但已经由用户创建的笔记保持普通 Nowen 笔记，不依赖插件继续存在。\n`);
write("docs/plugin-platform/prompt-pack-contribution.md", `# Prompt Pack Contribution\n\nV2.1 的 \`contributes.promptPacks\` 只描述 Prompt、输入字段、最小上下文声明、输出模式和 UI 平台。Contribution 永远拿不到 AI Provider Secret，也不能主动读取笔记。Host 只在用户触发时按声明提供 title/note/selection/tags 等最小上下文。\n\n单插件最多 100 个 Prompt，每个 Prompt 最大 64KiB。运行时 ID 使用 \`<pluginId>/<promptId>\`。\n`);

write("backend/tests/productivity-contribution.test.ts", `import assert from \"node:assert/strict\";\nimport fs from \"node:fs\";\nimport path from \"node:path\";\nimport test from \"node:test\";\nimport { fileURLToPath } from \"node:url\";\nimport { parsePluginManifest } from \"../src/plugins/manifest.js\";\n\nconst root = path.resolve(path.dirname(fileURLToPath(import.meta.url)), \"../..\");\nconst sample = JSON.parse(fs.readFileSync(path.join(root, \"examples/plugins/productivity-pack/manifest.json\"), \"utf8\"));\nconst options = { extensionsV21: true, currentVersion: \"1.6.0\" } as const;\n\ntest(\"official Productivity Pack is zero-code and zero-permission\", () => {\n  const manifest = parsePluginManifest(sample, options);\n  assert.equal(manifest.runtime, \"declarative\");\n  assert.deepEqual(manifest.permissions, []);\n  assert.equal(manifest.contributes?.noteTemplates?.length, 2);\n  assert.equal(manifest.contributes?.promptPacks?.length, 2);\n  assert.equal(\"main\" in manifest, false);\n});\n\ntest(\"unsafe static template content is rejected before install\", () => {\n  const invalid = structuredClone(sample);\n  invalid.contributes.noteTemplates[0].body = \"<script>alert(1)</script>\";\n  assert.throws(() => parsePluginManifest(invalid, options), /不安全内容/);\n});\n\ntest(\"oversized prompt is rejected before install\", () => {\n  const invalid = structuredClone(sample);\n  invalid.contributes.promptPacks[0].prompt = \"x\".repeat(65537);\n  assert.throws(() => parsePluginManifest(invalid, options), /64KiB/);\n});\n`);

write("frontend/src/lib/__tests__/pluginProductivityRegistry.test.ts", `import { describe, expect, it } from \"vitest\";\nimport { listPluginNoteTemplates, replacePluginNoteTemplates } from \"@/lib/pluginTemplateRegistry\";\nimport { listPluginPrompts, renderPluginPrompt, replacePluginPrompts } from \"@/lib/pluginPromptRegistry\";\nimport type { PluginContributionRecord } from \"@/lib/pluginApi\";\n\nconst records: PluginContributionRecord[] = [{\n  pluginId: \"acme.productivity\", publisher: \"acme\",\n  noteTemplates: [{ id: \"meeting\", name: \"Meeting\", contentFormat: \"markdown\", body: \"# {{title}}\", variables: [{ id: \"title\", type: \"string\", required: true }] }],\n  promptPacks: [{ id: \"rewrite\", name: \"Rewrite\", prompt: \"Tone: {{tone}}\", inputs: [{ id: \"tone\", type: \"string\", default: \"clear\" }], context: [\"selection\"] }],\n}];\n\ndescribe(\"plugin productivity registries\", () => {\n  it(\"namespaces note templates without mutating content\", () => {\n    expect(replacePluginNoteTemplates(records)).toBe(1);\n    expect(listPluginNoteTemplates()[0].runtimeId).toBe(\"acme.productivity/meeting\");\n  });\n  it(\"renders prompt inputs locally without exposing AI secrets\", () => {\n    expect(replacePluginPrompts(records)).toBe(1);\n    expect(renderPluginPrompt(listPluginPrompts()[0], {})).toBe(\"Tone: clear\");\n  });\n});\n`);

console.log("Productivity Pack V2.1 bootstrap applied");
