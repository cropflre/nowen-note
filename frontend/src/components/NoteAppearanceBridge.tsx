/**
 * Deprecated compatibility shell.
 *
 * Appearance styles now belong to the whole Nowen Note application. Per-note theme controls are
 * intentionally disabled so users have one clear appearance model: App Appearance Style + Theme
 * Mode. The metadata endpoint remains readable for backward compatibility with existing databases.
 */
export default function NoteAppearanceBridge() {
  return null;
}
