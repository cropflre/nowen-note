/**
 * Deprecated compatibility shell.
 *
 * Product direction changed from per-note appearance themes to application-wide appearance styles.
 * Keep this module temporarily so old imports do not break release/v1.5.0 while the visible settings
 * entry is removed. New appearance work belongs to App Appearance Style Capability / SkinSwitcher.
 */
export default function NoteThemePicker() {
  return null;
}
